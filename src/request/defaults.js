module.exports ={                             // valores por defecto para Axios, que va a pedir la api ya existente anteriormente
    url:'http://localhost:3000/api/',
    timeout:6000                              // 6 segundos de timeout
}