<!DOCTYPE html>
<html lang="es">

<%- include('../partials/head') %>

<body>
    <div class="container">
        <%- include('../partials/header__min') %>
        <main class="login__main">
            <h1 class="user-form-title">¡Hola! Ingresá tu Email</h1>
            <% if (typeof error != "undefined") { %>
                 <p style="color: red;"><%= error.msg %> </p>
            <% } %>
            <form class="register__form" action="/users/login" method="POST">
                <% if (typeof error != 'undefined') { %>
                    <p style="color: red;"><%= error %></p>
                <% } %>
                <div>
                    <input type="text" name="email" id="email" required placeholder=" " <% if (typeof old != 'undefined') { %>value="<%= old.email %>"<% } %> >
                    <label for="email">Email</label>
                </div>
                <div>
                    <input type="password" id="password" name="password" required placeholder=" ">
                    <label for="password">Contraseña</label>
                </div>
                <div class="login__remember">
                    <input id="rememberme" name="rememberme" value="remember" type="checkbox">Recuérdame
                </div>
                <button type="submit" class="submit-button">Ingresá</button>
                <a href="/password-recover">¿Olvidaste tu contraseña?</a>
                <a href="/users/register">Regístrate</a>
            </form>
        </main>
        <%- include('../partials/footer__min2') %>
    </div>
</body>

</html>