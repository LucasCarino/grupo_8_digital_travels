<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>

<body>
    <div class="container">
        <%- include('../partials/header__min') %>
        <main class="register__main">
            <h1 class="user-form-title">Completá tus datos</h1>
            <form class="register__form" action="/users/register" method="POST" enctype="multipart/form-data">
                <% if (typeof errors != "undefined") { %>
                    <% errors.forEach(error => { %>
                     <p style="color: red;"><%= error.msg %> </p>
                    <% }) %>
                <% } %>
                <div>
                    <input type="text" name="first_name" id="first_name" required placeholder=" " value="<%= old.first_name %>">
                    <label for="first_name">Nombre</label>                    
                </div>
                <div>
                    <input type="text" id="last_name" name="last_name" required placeholder=" " value="<%= old.last_name %>">
                    <label for="last_name">Apellido</label>
                </div>
                <div>
                    <input type="text" id="username" name="username" required placeholder=" " value="<%= old.username %>">
                    <label for="username">Nombre de usuari@</label>
                </div>
                <div>
                    <input type="email" id="email" name="email" required placeholder=" " value="<%= old.email %>">
                    <label for="email">E-mail</label>
                </div>               
                <div>
                    <input type="password" id="password" name="password" required placeholder=" " value="<%= old.password %>">
                    <label for="password">Contraseña</label>
                </div>
                <div>
                    <input type="password" id="passwordcheck" name="passwordcheck" required placeholder=" ">
                    <label for="passwordcheck">Confirmar contraseña</label>
                </div>
                <div>
                    <input type="file" id="image" name="image" required>
                    <label for="image">Imagen</label>
                </div>
                <button type="submit" class="submit-button">Continuar</button>
                <p>¿Ya tienes una cuenta?</p><a href="/users/login">Ingresa</a>
            </form>
        </main>
        <%- include('../partials/footer__min2') %>
    </div>
</body>

</html>